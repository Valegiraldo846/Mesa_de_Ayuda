services: 
  api:
    image: valegiraldo/mesa-ayuda
    ports:
      - "3000:8080"

    env_file: 
      - .env
    
    # environment:
    #   -DB_HOST=localhost
    #   -DB_PORT=3306
    #   -DB_NAME=helpdesk
    #   -DB_USERNAME=root
    #   -DB_PASSWORD=root

    #   -JWT_SECRET=mesaDeAyudaJWTSecret2026ClaveSeguraParaFirmarTokens123456789
    #   -JWT_EXPIRATION=900000
    #   -JWT_REFRESH_EXPIRATION=604800000

    mysql:
      image: mysql: 8
    environment:
      - MYSQL_USER=test-136
      - MYSQL_PASSWORD=test-136
      - MYSQL_DATABASE=test-136
    expose:
      -3306
    volumes:
    - mysql_test_data:/var/lib/mysql

  volumes:
    mysql_test_data
      